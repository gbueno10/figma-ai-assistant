# ⚠️ OPCIONAL: A API key da OpenAI é enviada pelo plugin Figma em cada requisição.
# Só defina aqui se quiser um fallback para desenvolvimento/testes.
# OPENAI_API_KEY=sk-your-openai-key

PORT=3000

# Optional comma-separated list of allowed origins for CORS (leave blank to allow all)
# CORS_ORIGIN=http://localhost:3300,https://your-domain.com
